<resources>
    <!-- Define a custom style for your buttons -->
    <style name="AppButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/white</item>
        <!-- Add other button attributes as needed -->
    </style>

    <!-- Define a custom style for your toolbar -->
    <style name="AppToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/white</item>
        <!-- Add other toolbar attributes as needed -->
    </style>
    <!-- Other styles may be defined here -->
    <!-- Ensure that your theme does not contain problematic dimension values -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Add your theme attributes here -->
    </style>
</resources>
